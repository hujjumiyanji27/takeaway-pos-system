name: takeaway_pro_pos
description: A professional takeaway POS system for Windows and Android.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  # path: ^1.8.3
  provider: ^6.1.2
  intl: ^0.18.1
  esc_pos_utils: ^1.1.0
  esc_pos_printer: 4.1.0
  flutter_blue_plus: ^1.24.3
  shared_preferences: ^2.2.2
  http: ^1.2.1
  url_launcher: ^6.2.5

  path: ^1.9.0

flutter:
  uses-material-design: true
